# HIMA 前端复刻

基于鸿蒙智行原始截图与用户提供的真机操作视频制作。手机内部已经改为可滚动、可点击、可变化的 DOM 组件，不再通过整张截图切换模拟交互。仓库保留“原版复刻”和“HarmonyOS 7 特性适配”两个互不污染的入口。

## 在线预览

- [HIMA 前端界面复刻](https://wuji3925.github.io/HIMA-frontend-replica/)
- [HIMA 沉浸光感适配预览](https://wuji3925.github.io/HIMA-frontend-replica/os7.html)

在线版本由 GitHub Pages 托管。推送到 `main` 分支后，两套预览会同步更新。

当前原版复刻的逐帧校准与回归结果见 [HIMA 原版复刻优化记录](FIDELITY_REBUILD.md)。HarmonyOS 7 的官方依据、产品洞察、组件映射、研发实现和验收门槛见 [HarmonyOS 7 设计语言与 HIMA 落地指南](HARMONYOS_7_DESIGN_LANGUAGE.md)，本轮网页实现记录见 [OS7 适配说明](OS7_ADAPTATION.md)。

## 打开方式

在本目录运行：

```bash
npm start
```

然后访问 `http://localhost:4173`。

### HarmonyOS 7 独立适配预览

访问 `http://localhost:4173/os7.html`，可体验五级材质语义、滚动联动顶栏、分层弹层、空间进退场、按压反馈、任务完成粒子、深浅色、单手可达模拟和减少动态效果。原版入口不受影响。网页只复现设计意图与交互机制，不宣称已经获得 ArkUI 原生系统材质、设备姿态或系统级空间动效能力。

## 已实现的交互

- 顶部频道支持点击或左右跟手滑动，松手后按距离和速度吸附到目标频道。
- 一级频道通过字号、字重与颜色区分选中态；蓝色下划线仅用于车型筛选栏。
- 首次频道加载显示参考视频中的圆环与“正在加载…”状态。
- 每个频道拥有独立纵向滚动位置；顶部标题栏和底部一级导航保持固定。
- 发现页车型栏吸顶，支持下拉、松开刷新、刷新中与刷新完成状态。
- 精选页头部叠在首屏宣传图上，滚动后过渡为白底深色文字。
- 底部「发现 / 精选 / 爱车 / 服务 / 我的」直接切换一级页面并保留浏览状态。
- 爱车页复刻车辆资源未就绪、下载弹层、下载进度、资源加载完成和车辆 Hero 恢复等状态。
- 爱车页上滑后告警收拢进标题栏。离线车控点击显示“车辆不在线”，不会伪装真实车控成功。
- 服务、我的页面均为真实长页面，可连续滚动并触发组件状态。
- 键盘方向键支持频道切换和页面滚动，`Esc` 可关闭弹层。

## 页面参数

地址栏支持直接指定一级页面与频道，例如：

```text
?page=discover&channel=1
?page=select&channel=3
?page=car
```

当前加载 `reference.js` 与 `reference.css`。前一版 `app.js`、`styles.css` 保留作为历史文件，入口不再加载。源图素材、裁切坐标与验证说明见 `REFERENCE_ACCEPTANCE.md`。

## 当前边界

### 按参考重建（2026-09-09）

- 原图尺寸 1176 × 2480，网页内部固定 392 × 826.667，外层整体等比缩放。
- 按原图恢复白底导航、双色实心底部图标、图片快捷入口、不等高双列信息流、动态帖子、广场品牌宫格、精选商品列表、爱车控制区、服务与个人中心分组列表。
- 删除前一版无依据的一级频道下划线、图片视差、悬浮回顶和底部切页横向进退动画。
- 原图宣传图、照片、系统装饰、独立导航图标从素材中提取；页面排版、文字、滚动与交互由 DOM 实现。
- 已核对 392×827、360×780 和桌面视口；已验证真实浏览器触摸事件下的横滑与纵滑、吸顶、位置保留、刷新、精选头部转换、分类返回、车辆告警收拢及资源弹窗。

本版本为组件化网页 Demo，卡片内容属于演示数据；局部图片来自原始截图裁切。`archived-screenshot-prototype` 与 `assets/v7-reference` 仅作上一版可回退备份，当前页面不会加载。此 Demo 不是 HDS/ArkUI 真机实现，也不代表真实车控、交易或后台服务已接入。
