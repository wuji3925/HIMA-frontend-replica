# HIMA × HarmonyOS 7 设计语言与落地指南

> 版本：v1.1（2026-09-20）
> 范围：鸿蒙智行手机端前台体验，覆盖产品原则、视觉语言、组件角色、交互动效、网页预览、ArkUI/HDS 原生映射与验收方法。  
> 状态：官方资料台账保留 09-17 核对记录；09-20 网页自然交互修订待用户视觉评审；原生系统材质、设备算力降级、真实智感握姿仍待 HarmonyOS 7 真机验证。

## 本次修订：从“特性数量”转向可理解、可打断的交互

用户反馈“不像鸿蒙且不自然”。本轮检查发现：精选标签受挤压、顶栏颜色按阈值跳变、弹层装饰和拖拽释放不衔接，以及精选搜索的业务语义错误。调整重点为布局层级、前景对比、连续运动及上下文一致，而非继续增加特效。

- 保留底部连续材质，不给每个页签增加玻璃底。
- 精选独立标题行与等宽频道行是本项目方案，不将其宣称为系统必选样式。
- 移除自绘扫光与扫码粒子；“系统提供能力”不等于“每个业务场景必须使用”。
- 频道和半模态采用同一可中断弹簧模型，保留当前位置与速度；减少动态直接完成。
- 以真实操作和可见状态验收，功能测试与用户视觉认可分别记录。

具体改动、演示参数、验证结果与未验证范围见 [09-20 自然交互修订](OS7_ADAPTATION.md#2026-09-20-自然交互修订)。以下官方资料表是历史研究记录，网页实现状态以本次修订及第13节为准。

## 1. 结论先行

HarmonyOS 7 的设计特征不是“全局玻璃化”，而是以人为中心、以内容为主体，用语义色彩、自适应布局、系统材质和自然动效建立可理解的空间层级。

HIMA 的适配原则：

1. 业务内容、车辆状态和高后果操作优先，视效不能降低确定性。
2. 只在悬浮操作与内容可能重叠，或临时浮层需与复杂背景分离时使用沉浸光感。
3. 材质使用系统枚举和组件语义，不手工堆叠多层模糊、高光和阴影。
4. 动效立即响应、跟手、可打断；减少动态或低算力时必须正常降级。
5. 粒子、流光和非线性形变是“信息反馈”，不是全页装饰。
6. 智感握姿只调整高频、单手难触达且低风险的组件；车锁、车窗、后备箱、付款和下单等位置保持稳定。

## 2. 证据与版本边界

| 来源 | 本轮用途 | 核对结果 |
| --- | --- | --- |
| [设计理念](https://developer.huawei.com/consumer/cn/doc/design-guides/design-concepts-0000001795698445) | One / Harmonious / Universe | 以人为本、物理隐喻、多设备一致与差异的平衡 |
| [色彩](https://developer.huawei.com/consumer/cn/doc/design-guides/color-0000001776857164) | 语义 Token、深浅色、对比度 | Primary / onPrimary / Brand / Container，默认颜色至少保障 3:1 对比度 |
| [鸿蒙黑体](https://developer.huawei.com/consumer/cn/doc/design-guides/font-0000001828772001) | 字体和字重 | HarmonyOS Sans，多语言、多字重、可变字体 |
| [布局基础](https://developer.huawei.com/consumer/cn/doc/design-guides/design-layout-basics-0000001795579413) | 安全区、设备尺寸 | 手机须避让状态栏、底部安全区与边缘 |
| [间隔参数](https://developer.huawei.com/consumer/cn/doc/design-guides/interval-parameter-0000002562577161) | 间距 Token | 手机左右 margin 16vp；优先 4/8vp 体系；卡片间 12vp |
| [控件概览](https://developer.huawei.com/consumer/cn/doc/design-guides/general_overview-0000001929599380) | 官方组件语义 | 导航、展示、操作、输入、选择、容器六大类 |
| [动效设计原则](https://developer.huawei.com/consumer/cn/doc/design-guides/animation-design-principles-0000001750237392) | 动效目标 | 自然流畅、简洁高效、快速响应、运动一致、隐藏等待、凸显品牌 |
| [动效属性](https://developer.huawei.com/consumer/cn/doc/design-guides/animation-attributes-0000001797117229) | 时长、曲线、帧率 | 简单变色 100ms；小范围 150ms；局部 200ms；全屏最长约 350ms |
| [转场动效](https://developer.huawei.com/consumer/cn/doc/design-guides/transition-animation-0000001750078488) | 层级与连续性 | 同层、上下层、跨层；共享元素、共享容器、共享动势 |
| [手势动效](https://developer.huawei.com/consumer/cn/doc/design-guides/gesture-animation-0000001797038213) | 点击、滑动、拖拽 | 按下立即反馈；滑动分跟手、离手瞬间、离手后；惯性继承手速 |
| [沉浸光感设计](https://developer.huawei.com/consumer/cn/doc/design-guides/immersivelight-0000002612101053) | 材质层级与场景 | 强/均衡/弱由系统映射；五级材质；重叠时才建立 Z 轴分离 |
| [ArkUI 沉浸光感简介](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/arkts-immersive-light-sense-overview) | 能力、组件范围、降级 | API 26.0.0 起；系统材质 + 空间动效；随算力、深浅色和用户设置自适应 |
| [沉浸光感典型场景](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/arkts-immersive-light-sample) | 标题滚动联动 | `NavDestination` + `BarStyle.STACK` + `GRADUAL_BLUR` 是原生重点路径 |
| [智感握姿](https://developer.huawei.com/consumer/cn/doc/design-guides/smart-reachability-0000002556657823) | 单手可达性 | 仅高频且难触达组件；整组移动保持顺序和间距；不移动正在按压的目标 |
| [最新粒子动画](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/arkts-particle-animation) | Particle 实现 | 发射器、颜色分布、生命周期、扰动场 |

用户给出的 `harmonyos-guides-V13` 粒子文档已归档，页面明确建议使用最新版本。本项目以上表中不带 `-V13` 的当前文档为实现依据，V13 仅用于确认概念演进。

## 3. 三层设计模型

### 3.1 One：人因与信息可读性

- 文字、图标和状态要清晰，必须让用户一眼看出主次与可交互性。
- 点击在按下瞬间开始反馈，不等待抬手。
- 车辆离线、门未关、充电、订单和支付状态使用明确文字，不仅用颜色或光效传达。
- 所有原生组件应继承字号放大、无障碍标签、焦点和减少动态设置。

### 3.2 Harmonious：材质、光影与物理运动

- 使用系统材质表达“内容在下、操作在上”，而不是为每个卡片增加玻璃质感。
- 入场使用减速曲线，出场使用加速曲线，始终在视野中的状态切换使用标准曲线。
- 跟手对象优先使用弹性模型，继承手速并允许中途打断。
- 流光、形变和粒子只为当前操作和状态变化服务。

### 3.3 Universe：自适应和多设备

- 交互语义与操作方式保持一致，布局根据手机、折叠屏、平板和分屏状态做差异化。
- 手机默认使用 16vp 左右边距，并避让顶部状态栏、底部系统导航和异形屏安全区。
- 折叠屏关键信息不放在折痕和转轴区。
- 当窗口变窄，优先保留主任务、主状态和高后果操作，次要信息分组或后置。

## 4. 视觉语言

### 4.1 色彩 Token

| 语义 | HIMA 网页 Token | 用法 |
| --- | --- | --- |
| Primary | `--font-primary` | 一级文字和图标 |
| Secondary | `--font-secondary` | 辅助文字、时间、说明 |
| Brand | `--os-brand` | 选中、高亮、核心操作 |
| Container Primary | `--container-primary` | 列表卡片、标准容器 |
| Container Secondary | `--container-secondary` | 输入框、次级容器 |
| Interactive Pressed | `--interactive-pressed` | 按压瞬间反馈 |

约束：颜色必须以语义 Token 映射深浅色；不使用反色文字 Token 直接覆盖普通背景；不反相照片、车辆渲染图和宣传图。

### 4.2 字体与排印

- 默认使用 HarmonyOS Sans，网页回退为 PingFang SC 和系统无衬线字体。
- 信息层级使用字号、字重、颜色共同表达，不仅靠字重。
- 里程、电量、温度和价格优先使用等宽数字特性，防止状态变化时宽度抖动。
- 中英混排保持基线和信息密度一致。

### 4.3 间距与容器

- 基础单位：4vp；常用节奏：8vp、12vp、16vp、24vp、32vp。
- 手机页面左右边距：16vp。
- 卡片间距：12vp；明确边界组件较大间距：16vp；普通间距：8vp。
- 主次文字上下间距：2vp；不用任意像素值破坏跨页节奏。

## 5. 沉浸光感材质角色

| 材质 | 官方语义 | HIMA 使用位置 | 网页预览 |
| --- | --- | --- | --- |
| `ULTRA_THIN` | 高透明浮动工具 | 顶部浮动导航、智感握姿辅助 | `--material-ultra-thin` |
| `THIN` | 较强透明，如搜索框 | 底部页签、标题区搜索 | `--material-thin` |
| `REGULAR` | 通用场景 | 少量通用悬浮操作 | `--material-regular` |
| `THICK` | 强模糊，如菜单 | 发布菜单、排序菜单 | `--material-thick` |
| `ULTRA_THICK` | 极强模糊，如弹窗 | 搜索半模态、消息、扫码、车辆资源弹层 | `--material-ultra-thick` |

原生实现要点：

- HDS 导航和 HDS 底部页签优先使用 UI Design Kit 的 `systemMaterialEffect`。
- 弹窗、菜单、Popup、Tips、半模态、Slider、Toggle 和 Select 可使用 ArkUI 指定能力。
- 其他普通组件的原生沉浸光感存在生效范围：主要在 `Navigation/NavDestination` 标题栏或底部 `TabBar` 中。网页任意区域的 CSS 模糊不等于原生可生效。
- 系统将用户强/均衡/弱设置自动映射到材质；原生代码不要分别写三套固定透明度。
- 系统材质已接管背景、边框、高光和阴影；如需自定义阴影，先关闭 `applyShadow`，禁止重复阴影。
- 根据 `uiMaterial.getGlobalMaterialLevel` 返回的设备材质级别验证降级，不自行猜测设备性能。

## 6. 动效与手势参数

### 6.1 时长基线

| 场景 | 时长基线 | HIMA 应用 |
| --- | --- | --- |
| 颜色、按压态 | 100ms | 按钮、页签、菜单项立即反馈 |
| 小范围图标/开关 | 150ms | 点赞、收藏、切换图标 |
| 局部组件 | 200–250ms | 频道、筛选、菜单 |
| 长距离或全屏 | 不超过约 350ms | 半模态、跨页转场 |

### 6.2 曲线

- 标准曲线：`cubic-bezier(0.40, 0.00, 0.20, 1.00)`，用于 Tab、Switch 等始终在视野中的对象。
- 减速入场：`cubic-bezier(0.00, 0.00, 0.40, 1.00)`，用于弹层、菜单出现。
- 加速出场：用于原本在视野中且需离开的对象。
- 弹性曲线：用于跟手、滚动和可打断交互。智感握姿原生参数为 `interpolatingSpring(velocity 0, mass 1, stiffness 200, damping 17)`；屏幕外移入/移出使用 stiffness 170。网页 cubic-bezier 只是视觉近似，不宣称与原生物理模型等价。

### 6.3 手势交互

| 手势 | 设计洞察 | 当前适配 |
| --- | --- | --- |
| 点击 | 按下就反馈，不等抬手 | 底部导航、标题按钮、菜单项按压缩放与事件色 |
| 滑动 | 跟手、继承手速、离手减速 | 纵向内容、横向频道、Banner |
| 拖拽 | 使用显控比和阻尼告知边界 | 半模态拖拽下滑关闭、下拉刷新 |
| 翻动 | 有明确限位和成功/取消结果 | 频道和横向图片轨道吸附 |
| 夹捏 | 只用于可缩放内容 | 当前 HIMA 主页无必要，不为“丰富交互”强行增加 |

## 7. 空间动效与粒子政策

ArkUI 沉浸光感定义三类空间动效：

1. **非线性形变**：适合 Dialog、ActionSheet、CustomDialog 和 Menu 的出现过程。
2. **边缘流光**：用光流引导视线并标记弹层边界，不做常驻循环。
3. **粒子动画**：以粒子光点传递信息变化；官方沉浸光感示例重点支持 Slider。

HIMA 使用规则：

- 允许：扫码识别成功、OTA 升级节点、充电目标达成、里程成就等短暂的状态变化。
- 谨慎：日常轻量点赞、收藏，默认只用 100–150ms 属性反馈。
- 禁止：背景持续粒子、列表滚动尾迹、车控按钮常驻流光、用粒子代替文字状态。
- 减少动态、弱档、关闭光感时，粒子直接禁用，保留文字和颜色反馈。

09-17 网页曾以 12 个粒子模拟扫码成功；09-20 已移除。当前扫码使用持久文字与图标状态，不把未经验证的自绘粒子当作 ArkUI `Particle` 等效实现。

## 8. 智感握姿

接入门槛必须同时满足：

- 当前页高频操作。
- 默认位置单手难触达。
- 迁移后不会导致状态误解、误触或高后果操作。
- 能在未识别握姿、权限关闭或低版本下恢复默认布局。

组件迁移时：

- 整组迁移，保留顺序、间距、垂直高度和左右对称边距。
- 功能、状态、禁用态和可访问名称与原组件一致。
- 不移动正在按下的组件，不打断滑动、输入、下载和车控操作。

当前网页只手动模拟左手/双手/右手，且在 pointer 按压期间排队等待迁移。原生版需再根据当前 SDK “获取用户动作”接口和权限进行实现与真机验收。

## 9. 官方组件全景与 HIMA 适配

| 类别 | 官方控件 | HIMA 洞察与适配 |
| --- | --- | --- |
| 导航类 | 底部页签、子页签、标题栏、导航点 | 一级底栏一只连续 THIN 表面；频道使用字号/字重/颜色区分；内容从顶部下方经过 |
| 展示类 | 文本、分隔器、子标题、进度条、索引条、滚动条、新事件标记、即时反馈、气泡提示、数据可视化、二维码、时钟、图片、空白 | 数值、车况和进度始终优先可读；Toast 只作短反馈；空状态给出下一步操作 |
| 操作类 | 按钮、下拉按钮、状态按钮、操作块、工具栏、核心操作栏、菜单、文本选择菜单 | 核心行动保持高对比实体表面；菜单使用 THICK；网页命中区不低于 44px |
| 输入类 | 文本框、搜索框、数字加减、图案锁 | 搜索半模态使用 ULTRA_THICK，内部输入框保持实体容器，避免材质嵌套 |
| 选择类 | 勾选、开关、单选框、评分条、滑动条、选择器、分段按钮 | 主题、光感、握姿与减少动态使用明确选中态；原生优先标准组件 |
| 容器类 | 列表、弹出框、半模态面板 | 列表和卡片默认实体；弹出框/半模态使用 ULTRA_THICK；必须有关闭、焦点回送和焦点限定 |

## 10. HIMA 页面级落地矩阵

| 页面 | 主任务 | OS7 适配 | 禁止项 |
| --- | --- | --- | --- |
| 发现 | 浏览、搜索、频道切换 | 滚动联动顶栏、THIN 搜索、频道跟手、排序 THICK 菜单 | 每张信息卡材质化、背景常驻粒子 |
| 精选 | 商品与软件服务浏览 | 宣传图上工具保持对比，滚动后回到标准标题层 | 为商品价格添加低对比光效、隐藏交易条件 |
| 爱车 | 读取车况、车控 | 状态优先、警告收拢、资源弹窗 ULTRA_THICK | 车控随握姿移动、离线时伪装成功、常驻流光 |
| 服务 | 充电、保养、门店 | 语义进度、实体卡片、统一 8/12/16vp 间距 | 将服务状态仅用光效表达 |
| 我的 | 身份、订单、设置 | 语义容器、二维码半模态、深浅色 | 将隐私或身份信息放入装饰光效 |

## 11. 产品到研发的实现路径

### 11.1 产品定义

1. 识别用户任务、交互频次、风险等级和设备环境。
2. 对每个组件标记：信息层级、是否重叠、材质角色、手势、动效、降级、可访问性和验收指标。
3. 高后果操作设立额外门禁：位置稳定、文字确认、状态可追溯、失败可理解。

### 11.2 设计交付

- 用 UI Design Kit 的 Token 和标准组件出图，记录组件变体和状态。
- 交付深/浅色、强/均衡/弱/关闭、正常/减少动态、左/右/双手、正常/失败/禁用/离线状态。
- 动效标注必须包含触发条件、时长/物理参数、可打断行为和降级结果。

### 11.3 ArkUI / HDS 实现

- 导航和底部页签优先 HDS；更多弹层和自定义组件使用 ArkUI 沉浸光感能力。
- 顶部滚动联动优先使用 `Navigation/NavDestination`、`BarStyle.STACK` 和渐变模糊，不手工叠加全局模糊图层。
- 主题和语义色使用系统 Token，不在业务组件中复制两套颜色。
- 使用系统减少动态和材质等级，禁用程序内强制开启最高特效。

### 11.4 网页 Demo 边界

CSS `backdrop-filter`、透明度和光影仅用于表达产品方向。它无法代替系统材质的折射、自动反色、设备算力降级和原生空间动效。网页验收只证明信息架构、组件角色、交互状态和方向可行。

## 12. 验收清单

### 功能与状态

- [ ] 五个一级页面、四个频道、搜索、排序、弹层、下拉刷新和车辆资源流程无回归。
- [ ] 每个组件正常、按压、选中、禁用、加载、成功和失败状态可识别。
- [ ] 弹层关闭后焦点回到触发组件，Tab 焦点不越界。

### 视觉与材质

- [ ] 深浅色文字、图标、品牌色和容器使用语义 Token，照片不反相。
- [ ] 顶部 `ULTRA_THIN`、底部 `THIN`、菜单 `THICK`、弹层 `ULTRA_THICK` 角色清晰。
- [ ] 普通卡片、列表和车控按钮没有被全局材质化。
- [ ] 原生版无重复模糊、重复阴影和超出生效范围的材质调用。

### 动效与性能

- [ ] 按下立即反馈，短反馈在 100–150ms，局部切换在 200–250ms，长距离转场不超过约 350ms。
- [ ] 跟手、拖拽和滑动不被非必要动画阻塞，可在中途打断。
- [ ] 减少动态时粒子、流光、形变和平滑滚动降级。
- [ ] 低材质级别下层级、对比度和功能不变；真机检查帧率、功耗和过度绘制。

### 车辆安全

- [ ] 离线不显示伪成功；高后果操作不随握姿移动。
- [ ] 状态变化有文字、图标或数值的持久反馈，不仅有动效。
- [ ] 交易与车控最终验收覆盖误触、弱网、超时、重试、取消和重复提交。

## 13. 当前实现状态

| 特性 | 网页状态 | 原生状态 |
| --- | --- | --- |
| 语义色彩与间距 | 已适配 | 待 UI Design Kit Token 复核 |
| 顶部滚动联动 | 已适配 | 待 `NavDestination` 真机 |
| 底部连续 THIN 材质 | 已适配 | 待 HDS Tabs 真机 |
| 菜单/弹层材质分级 | 已适配 | 待 ArkUI 系统材质 |
| 连续运动/可打断手势 | 频道与半模态使用网页弹簧；移除自绘扫光和夸张形变 | 待原生运动参数与真机对照 |
| 粒子信息反馈 | 当前不启用；扫码改为稳定状态反馈 | 仅在业务确有必要时评估原生支持 |
| 智感握姿 | 已手动模拟并保护按压期 | 待传感器、权限和真机 |
| 深色/减少动态/无模糊降级 | 已适配 | 待多设备、低算力与折叠屏 |

本文档与 [OS7_ADAPTATION.md](./OS7_ADAPTATION.md) 共同构成设计语言与当前代码的可追溯基线。

## 14. 09-17 历史网页实测记录

本节对应旧版本；粒子相关结果不代表 09-20 当前行为。新版本验证见适配说明。

2026-09-17 使用真实 Chromium 对当前提交进行手机画布回归：

- 390 × 848：浅色/深色、顶部滚动联动、底部连续材质、搜索半模态、关闭退场、服务页扫码和原版入口均通过；控制台 0 error、0 warning。
- 粒子反馈：均衡 + 完整动效触发 12 枚有限生命周期粒子，760ms 后全部移除；弱档或减少动态时触发数量为 0，完成文字仍保留。
- 智感握姿模拟：左手时底栏与辅助组向左，右手时向右；频道辅助保持顺序、高度和禁用态，车控不参与迁移。
- 360 × 780：页面无横向溢出，手机画布等比缩放为约 336 × 709；设置、正文与预览可纵向浏览。
- 原版隔离：`index.html` 未加载 OS7 预览层，五个一级页面和四个发现频道仍由原版脚本提供。

以上只证明网页交互和降级逻辑可用。ArkUI 系统材质、空间动效、算力分级、真实握姿和折叠屏仍需在 HarmonyOS 7 原生工程及真机验收。
