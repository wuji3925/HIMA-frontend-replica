# HarmonyOS 7 独立适配预览

入口：[/os7.html](./os7.html)。原版：[/index.html](./index.html)。

新增页复用现有 `reference.js`、`reference.css` 和图片素材；通过仅在新入口加载的 `os7.js`、`os7.css`、`os7-top.css` 增强。原入口和原版脚本、样式没有修改。手机内部仍采用 392 × 826.667 的等比画布。

## 依据与状态

2026-09-17 重新核对了 HarmonyOS 当前官方设计与开发文档，完整结论、组件全景、产品/研发路径和验收清单见 [HARMONYOS_7_DESIGN_LANGUAGE.md](./HARMONYOS_7_DESIGN_LANGUAGE.md)。

- [设计理念](https://developer.huawei.com/consumer/cn/doc/design-guides/design-concepts-0000001795698445)：One / Harmonious / Universe，要求人因可读、物理隐喻和多设备一致与差异的平衡。
- [色彩](https://developer.huawei.com/consumer/cn/doc/design-guides/color-0000001776857164)、[间隔参数](https://developer.huawei.com/consumer/cn/doc/design-guides/interval-parameter-0000002562577161)、[鸿蒙黑体](https://developer.huawei.com/consumer/cn/doc/design-guides/font-0000001828772001)：语义 Token、深浅色、4/8vp 间距和 HarmonyOS Sans 是基础层。
- [动效设计原则](https://developer.huawei.com/consumer/cn/doc/design-guides/animation-design-principles-0000001750237392)、[动效属性](https://developer.huawei.com/consumer/cn/doc/design-guides/animation-attributes-0000001797117229)、[手势动效](https://developer.huawei.com/consumer/cn/doc/design-guides/gesture-animation-0000001797038213)：按下立即反馈，时长按 100/150/200/350ms 范围和运动尺度分级，跟手运动继承手速。
- [沉浸光感设计](https://developer.huawei.com/consumer/cn/doc/design-guides/immersivelight-0000002612101053)、[ArkUI 沉浸光感简介](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/arkts-immersive-light-sense-overview)：从 API 26.0.0 开始，原生能力由系统材质和空间动效构成，并受组件生效范围、用户设置和设备算力约束。
- [智感握姿](https://developer.huawei.com/consumer/cn/doc/design-guides/smart-reachability-0000002556657823)：仅调整高频且单手难触达操作，不移动正在按压的目标。
- [最新粒子动画](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/arkts-particle-animation)：以发射器、颜色分布、生命周期和扰动场控制信息反馈。用户给出的 `-V13` 文档已归档，本次以当前文档为准。

本页是**网页交互预览**，不是已接入系统材质、传感器或已通过 HarmonyOS 原生验收的应用。模糊、透明度、圆角、时间和位移都是该网页的演示取值，不是官方 HDS 标注值；强弱开关也不是读取或修改用户系统设置。

## 2026-09-17 完整设计语言适配

- 样式 Token 从“模糊强弱”升级为 `font / brand / container / interactive` 语义色和 `ULTRA_THIN / THIN / REGULAR / THICK / ULTRA_THICK` 五级材质角色。
- 顶部标题区保持内容滚动联动，底部页签使用 THIN，搜索半模态和对话层使用 ULTRA_THICK，菜单使用 THICK，单手辅助使用 ULTRA_THIN。
- 按压事件色使用 100ms，小范围缩放 150ms，菜单 250ms，半模态 350ms；入场使用减速曲线，出场使用加速曲线。
- 菜单和半模态增加一次性非线性形变与边缘光过渡；关闭、弱档和减少动态时降级。
- 扫码模拟识别成功时增加有限生命周期粒子，仅表达信息变化，不做页面背景效果。
- 滚动区在越过 8px 后向顶部层提供轻量分层信号，不模糊标题与图标前景。
- 智感握姿依然是手动模拟；整组底部导航和低风险频道辅助保持水平迁移，按压期间延后变更，车控与交易不迁移。

## 组件映射

| 部位 | 页面行为 | 原生映射与范围 |
| --- | --- | --- |
| 一级底部页签 | 一只连续悬浮表面；内容从下方经过，末尾留出避让空间 | HDS 底部页签 / ArkUI 底部 TabBar，THIN 方向 |
| 状态栏与顶部导航 | 连续磨砂背景、下缘渐隐；内容从其下方滚过，文字与按钮前景保持清晰 | 导航区域整体背景的网页转译；覆盖系统状态栏安全区的原生行为需另行验证 |
| 搜索 | 半模态展示本地搜索结果，输入、跳转、取消、下拉关闭可用 | BindSheet 等系统半模态，ULTRA_THICK 方向；内部输入框为普通实体组件，避免材质嵌套 |
| 内容排序 | 推荐信息流显示排序菜单并执行本地排序 | Menu，THICK 方向 |
| 车辆资源弹窗 | 保持两个文字操作，增加局部表面与入场变化 | Dialog，ULTRA_THICK 方向 |
| 单手辅助 | 用户手动选择左右手，辅助频道组保持顺序、间距与高度，只水平移动 | 握姿识别结果的网页模拟；辅助副本属于产品方案，原生需结合 HDS 支持区域实现或采用实体表面 |
| 车控 / 交易 | 实体按钮，位置不随握姿切换，离线状态保持明确 | 不增加迁移与材质行为 |
| 普通卡片 / 列表 / 宫格 | 保持普通内容表面，深色模式按语义调整颜色 | 不逐项设置沉浸材质 |

## 顶部及组件补齐（2026-09-09）

本节保留首次组件适配记录；其中仅按钮模糊、搜索自身模糊及顶部不设连续背景的处理，已由下节 2026-09-10 修订替代。

结合项目 `06_鸿蒙智行沉浸光感设计语言_v1.0.md` 与 `08_竞品底部导航与顶部工具组件_V7适配规则.md`，并重新核对官方[沉浸光感简介](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/arkts-immersive-light-sense-overview)及[开启沉浸光感](https://developer.huawei.com/consumer/cn/doc/HarmonyOS-Guides/arkts-immersive-light-sense-enable)。设计指南旧入口本次仍超时，未把不可访问正文当作本次已验证来源。

- **开放标题与页签**：不增加统一玻璃横条。顶部频道可横向滚动，选中长标签后自动移入可见区域；保留字号和字重的选中差异。
- **顶部工具**：发现的加号、消息，精选的菜单、搜索，爱车/服务的扫码、消息，我的二维码、扫码、消息均保留。44px 网页命中区包围 36px 视觉圆面。均匀背景用轻质普通表面；精选宣传区的独立工具才使用 ULTRA_THIN 方向的背景扩散。
- **顶部过渡**：精选首屏使用独立渐变，滚动时回到可读的标题底色；渐变不叠加模糊，标题不在材质按钮内。
- **搜索**：页面搜索框为 THIN 方向；打开搜索后，输入区保持实体表面，不与半模态材质嵌套。按压收缩、输入焦点、取消与结果跳转可用。发现页搜索属于网页布局转译，原生需纳入受支持的标题区域或采用标准实体搜索组件。
- **筛选与布局**：车型采用标准实体胶囊和蓝色选中态，保留吸顶和横向滚动。排序菜单使用 THICK 方向；不为每个筛选项增加材质。
- **弹层与即时反馈**：发布菜单、消息、扫码预览、个人二维码示意、草稿表单与 Toast 使用对应菜单/半模态/即时反馈层级。发布草稿仅在当前页面内存保留，刷新即清空；不会向社区发布。扫码不访问摄像头，二维码图标不是可识别二维码。
- **范围保护**：本轮底部导航样式文件 `os7.css` 保持原样，车控与交易按钮不迁移、不改为材质按钮。普通卡片、宫格和列表不玻璃化。

补充验证：菜单到草稿及关闭后焦点返回、草稿本页恢复、消息关闭、二维码关闭、模拟扫码、分类返回、精选首屏与滚动状态、末尾频道完整可见、车型栏吸顶、排序、深色搜索、关闭材质、减少动效、360px 窄屏、车辆标题工具无越界。截图为 `../output/playwright/os7-top-*.png`。这些检查不构成真机帧率、功耗或原生系统材质验收。

## 连续顶部磨砂背景修订（2026-09-10）

按用户确认的方向，顶部不再仅给独立按钮加材质。五个一级页面和分类页将滚动区域延伸至屏幕顶部，首屏内容仍留出 98px，保留原有初始排版；上滑后内容可以实际进入状态栏和标题栏下方。

- 单独的 `.top-backdrop` 与滚动内容同属一个页面层级，覆盖顶部 118px，下缘渐隐。它位于文字/图标下方，不模糊前景。截图验证曾发现标题容器内的伪元素只能透出未模糊文字，因此最终采用页面同级背景层，实际照片颜色可被扩散。
- 移除顶部按钮上的重复背景滤镜；会经过顶部下方的搜索框也不再保留独立模糊，避免多层叠加。按钮按压反馈和点击范围保持可用。
- 浅色、深色及强/均衡/弱使用不同网页演示取值。关闭光感或浏览器不支持背景滤镜时，顶部回退为 98px 实色背景，内容位置不跳动。
- 筛选栏吸顶位置调整为 110px，搜索结果滚动落点避让标题与筛选栏；下拉刷新指示保持在标题下方。
- 状态栏原始网络图标通过前景透明化滤镜去除白色底片，保留清晰图标，避免其覆盖连续磨砂背景。
- 验证覆盖：我的首屏/上滑/深色、发现上滑与筛选吸顶/排序/搜索结果定位、精选首屏与上滑、分类列表上滑/返回、服务上滑、车辆操作位置不随握姿移动、横向拖动切频道、下拉刷新恢复、减少动效和 360px 窄屏。浏览器控制台无错误或警告；截图为 `../output/playwright/os7-frost-*.png`，其中含 `probe` 的文件仅为调试过程。
- 原版 `index.html`、`reference.js`、`reference.css` 以及底栏样式文件 `os7.css` 的文件校验值保持不变。本修订仍是网页体验演示，不宣称等同于原生 HDS 材质或已通过真机验收。

## 可操作的预览

- 外观：浅色、深色、跟随浏览器系统偏好。
- 光感：强、均衡、弱、关闭。关闭时使用实体底色；浏览器不支持背景滤镜时降级到实体表面。
- 握姿：左手、双手、右手。单手时出现与顶部频道状态同步的辅助操作；按压期间延迟位置迁移，不移动正在操作的目标。车辆与交易操作不迁移。
- 减少动态效果：压缩网页过渡；系统要求减少动态效果时自动遵守。原生触摸滚动仍由浏览器负责。
- 深色模式不会反相宣传图、照片或车辆图片。
- 设置写入当前页面的 URL，可复制分享指定预览状态；不保存账号数据、不读握姿传感器。

示例：`/os7.html?page=discover&channel=0&theme=dark&light=balanced&grip=right&motion=reduce`。

## 交付检查

使用浏览器检查深浅色、手动握姿、搜索与排序、材质关闭、减少动效、车控位置、弹层、手机窄屏设置区及原版回归。截图位于 `../output/playwright/os7-*.png`。原生系统材质分档、真实握姿识别、设备性能与折叠屏布局不作为本网页已验证能力。
