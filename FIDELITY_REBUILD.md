# HIMA 原版复刻优化记录

目标顺序：先完成原版界面与交互复刻，再在同一套组件和状态机上继续优化 HarmonyOS 7 沉浸光感预览。OS7 专属材质文件暂不作为本阶段调整对象。

## 当前证据等级

- 截图与视频：已验证；视频为 608 × 1280、30fps、35.6 秒。
- 网页首屏：已建立 392 × 827 对照图；发现页优化前参考/实现 SSIM 为 0.855870，第一轮回归截图为 0.857991。该数值受卡片内容和图片差异影响，只作为后续趋势基线。
- APK：尚未提供，当前属于静态证据级复刻，不能把网页参数宣称为原 App 原生参数。

## 第一轮：频道切换与加载

逐帧观察视频 3.6–4.9 秒：

1. 点击“动态”后，频道标题和内容在相邻帧内直接切换，没有横向推屏动画。
2. 加载态持续约 1 秒，而不是原实现中的 420ms。
3. 动态页先恢复车型筛选栏，内容区域继续显示加载状态，随后信息流出现。
4. 横向手势仍保留跟手位移和松手吸附；仅点击行为改为即时切换。

对应修改位于 `reference.js` 与 `reference.css`。后续轮次继续校准纵向滚动、吸顶、下拉刷新、底部导航和各页面状态。

本轮浏览器回归：点击频道的轨道时长为 0ms；手势横滑仍产生 150–260ms 的速度相关吸附；300ms 截图保留筛选栏与加载态，1200ms 截图进入内容态；控制台无错误或警告。

## APK 到位后的增强

APK 静态分析可用于核对图片、矢量图标、颜色、尺寸、文案和页面资源命名。若 APK 可在授权设备或模拟器运行，再采集同版本的截图、录屏与 UI 层级；登录、车控、交易和外部服务只检查安全边界，不执行真实操作。
