# APK 静态证据记录

本页记录用于“HIMA 前端界面复刻”的 APK 静态核对结果。这些结果不等同于真机行为实测。

## 基线

- 文件：`app.huawei.motor.apk`
- 应用：鸿蒙智行
- 包名：`app.huawei.motor`
- 版本：`3.0.2.320` (`300023200`)
- Android 最低/目标 SDK：26 / 33
- SHA-256：`f6417c31083500de31f4b2125767ffb69f3ef4d295c39ea753d470803fc377be`
- 技术线索：APK 内含 ArkUI-X 运行时、`modules.abc` 与 XSdk 资源。

## 已用于页面的可确认参数

| 项目 | APK 证据 | 网页处理 |
|---|---|---|
| 底部导航图标 | 24vp 双色矢量资源 | 改为同几何路径的内联 SVG，不再使用带背景的截图切片 |
| 导航选中色 | 主色 `#0A59F7`，辅色 `#94B6FA` / `#9DBCFB` | 按原图标变体切换两组颜色 |
| 导航未选色 | 35% 黑 + `#C7C7C7` | 按原资源的双色层级处理 |
| 按压缩放 | `1.0 → 0.9 → 1.0`，单段 80ms，曲线 `(0.3, 0.1, 0.15, 0.85)` | 用于底部导航与高频操作组件 |
| 动态加载齿轮 | 380ms 线性无限旋转 | 用于频道首次加载层 |
| 下拉刷新旋转 | 通用加载资源 1500ms 线性旋转 | 下拉刷新与频道加载分开设置 |
| 底部弹层进出 | 300ms，进入减速，退出加速 | 作为后续车辆资源下载弹层的交互验收基线 |

## 边界

- 本轮没有安装 APK，也没有进入登录、车控、订单或支付流程。
- APK 本体、反编译代码和整套原始素材不纳入公开网页仓库。
- 页面内容、服务端数据、登录态与真车状态仍需后续用目标设备与同版本应用复核。
